Rincian Fungsi Masing-Masing Library:

Package | Kategori | Alasan & Fungsi
--- | --- | ---
lucide-react | Icons | Set ikon modern dan bersih (icon target, kalender, centang task, AI spark, setting).
framer-motion | Animation | Memberikan animasi mikro halus (efek fade-in saat task hari ini dimuat, smooth progress bar, dan efek complete task tanpa mengagetkan user).
@tanstack/react-query | State & API | Mengelola caching data API backend, otomatis re-fetch jika ada perubahan data, dan mendukung optimistic UI updates.
axios | HTTP Client | Memanggil REST API FastAPI Backend dengan interceptor token/error bawaan.
react-hook-form + zod | Form & Validation | Menangani form input target baru (Goal Title, Target Date, Daily Available Time) dengan validasi tipe data cepat sebelum dikirim ke AI.
clsx + tailwind-merge | Styling Helper | Membuat gabungan kelas Tailwind CSS dinamis (standar fungsi cn() Shadcn UI).